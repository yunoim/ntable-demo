# package.json 스테이징 시 package-lock.json 자동 동기화 (2026-04-20 Railway 배포 실패 재발 방지)
if git diff --cached --name-only | grep -q '^package\.json$'; then
  echo "[pre-commit] package.json 변경 감지 — package-lock.json 동기화"
  npm install --package-lock-only --no-audit --no-fund
  git add package-lock.json
fi
