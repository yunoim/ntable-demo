# 2026-04-21 · ntable-app changelog

## Batch 1 (feat/batch-1-ux-foundation)

- **T-04** · `create.html`에서 죽은 ⚙ Advanced 패널(CSS·HTML·JS) 전면 제거. `#advancedPanel`, `#advancedBtn`, `advancedOverrides`, `markDirty`, `RADIO_MAP`, `adv_*` 전 심볼 삭제.
- **T-03** · 닉네임 실시간 중복 체크. 서버 `GET /api/rooms/:code/nickname-check?n=&uuid=` 추가, join.html 닉 step blur 300ms debounce → `.nick-conflict` slot 에 '본인 이어가기' / '이어받기(복구 요청)' CTA 표시.
- **T-02** · 모임장·게스트 위저드 분리. `join.html`의 `isHost` 분기 전면 제거 (grep 0건), 모임장 접근 시 `/room/:code/host` 로 즉시 redirect. `create.html` → 생성 후 `/host` 직행. `PATCH /api/rooms/:code/display` 에 phase 가드 추가 (탐구 진입 후 `DISPLAY_LOCKED` 403).
- **T-01** · 게스트 위저드 1단계(닉네임)로 축소 + lazy prompt 도입. 팩별 `wizard_required: ['nickname']` (playlist-share 만 `['nickname','playlist']`) / `wizard_prompt_after_join` 추가. `join.html` `rebuildSteps()` 가 required 기준으로 동작, 이모지 미입력 시 랜덤 풀에서 자동 배정. `guest.html` 소개 탭 상단에 `#lazyPromptCard` (미입력 필드 있을 때만) — 펼치기·저장·나중에. 저장은 `PATCH /api/rooms/:code/me/profile`. *숨은 제약 dating gender-gating fallback 은 별도 이슈.*
- **hotfix** · `gender` 컬럼 VARCHAR(10) → VARCHAR(20). '선택 안 함'(`unspecified`, 11자) 입력 시 DB 오버플로우로 입장 실패하던 버그. users + room_members 양쪽 ALTER.
